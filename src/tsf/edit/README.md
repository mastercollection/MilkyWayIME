# Edit Session Layer

Composition start, update, and end session helpers belong here, together with
the runtime `TextEditSink` that translates queued engine edit operations into
real `ITfEditSession` writes.
